{
  "entities": {
    "orders": {
      "title": "Orders",
      "description": "Customer pre-orders for adulteration diagnostic test kits",
      "type": "object",
      "properties": {
        "orderId": {
          "type": "string",
          "description": "Auto-generated unique order identifier"
        },
        "customerName": {
          "type": "string",
          "description": "Customer full name"
        },
        "phone": {
          "type": "string",
          "description": "Customer contact phone number"
        },
        "email": {
          "type": "string",
          "description": "Customer email address"
        },
        "deliveryAddress": {
          "type": "string",
          "description": "Full shipping delivery address including city, state, pincode"
        },
        "items": {
          "type": "array",
          "description": "Ordered test kit products with tier, quantity, and price",
          "items": {
            "type": "object",
            "properties": {
              "productId": { "type": "string" },
              "productName": { "type": "string" },
              "tier": { "type": "string", "enum": ["single-use", "multi-use"] },
              "quantity": { "type": "number" },
              "pricePerItem": { "type": "number" }
            },
            "required": ["productName", "tier", "quantity", "pricePerItem"]
          }
        },
        "orderTotal": {
          "type": "number",
          "description": "Total order amount in Indian Rupees (₹)"
        },
        "orderStatus": {
          "type": "string",
          "enum": ["Pending", "Confirmed", "Fulfilled", "Cancelled"],
          "default": "Pending"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp of order submission"
        },
        "notes": {
          "type": "string",
          "default": "",
          "description": "Admin notes for order verification and fulfillment tracking"
        }
      },
      "required": [
        "customerName",
        "phone",
        "email",
        "deliveryAddress",
        "items",
        "orderTotal",
        "orderStatus",
        "createdAt"
      ]
    },
    "messages": {
      "title": "Messages",
      "description": "Customer contact form submissions with optional photo attachment",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Sender full name"
        },
        "email": {
          "type": "string",
          "description": "Sender email address"
        },
        "phone": {
          "type": "string",
          "description": "Optional contact phone number"
        },
        "message": {
          "type": "string",
          "description": "Inquiry or message text"
        },
        "imageUrl": {
          "type": "string",
          "description": "Optional Firebase Storage URL of attached test strip or milk sample photo"
        },
        "status": {
          "type": "string",
          "default": "New",
          "description": "Status of the message submission"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of message submission"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO timestamp of message submission"
        }
      },
      "required": [
        "name",
        "email",
        "message",
        "createdAt"
      ]
    }
  },
  "firestore": {
    "/orders/{orderId}": {
      "schema": "orders",
      "description": "Customer pre-orders for diagnostic kits"
    },
    "/messages/{messageId}": {
      "schema": "messages",
      "description": "Customer messages and photo inquiries"
    }
  }
}
