rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Orders collection:
    // Allow customer pre-order submissions and admin management of orders.
    match /orders/{orderId} {
      allow read, write: if true;
    }
  }
}
